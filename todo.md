Visual Changes:
- update ui to move resign button to a different position not next to roll button 
    - add a confirmation to the resign flow where user can confirm or cancel
- (FUTURE) visualize the rolling of the dice and an actual die face
- add chat feature to the move log div (can be move log or a caht to talk to opponent)
- (FUTURE) animations for house of netting, bonus, waters of chaos, and being bourne off.
- remove the additional "exited" boxes and use only area next to user name: should have 5 circles, 
    and each start empty, for every piece the user gets bourne off, fill 1 with color


Performance Changes:
- keyboard shortcut for rolling (spacebar to roll)

Logic Changes:
- allow users to chat to each other in game
- add a confirmation to the resign flow where user can confirm or cancel
- add "rematch" || "new game" option on game finish screen