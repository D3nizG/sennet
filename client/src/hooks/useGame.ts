/**
 * Re-export useGame from GameContext.
 * This file exists so existing imports like `import { useGame } from '../../hooks/useGame'`
 * continue to work without modification.
 */
export { useGame } from '../context/GameContext';
